#include<bits/stdc++.h>
using namespace std;

int main(){
    vector<int>arr = {2,2};
    int m =*max_element(arr.begin(),arr.end());
    vector<int>hsh(m,0);
    hsh[0]=1;
    for(int i = 1;i<arr.size();i++){
        mpp[i]
    } 
}